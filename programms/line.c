#include "point.h"
#define line_h_
struct line{
    point *start;
    point *end;
};

point_t *line_new(point *p1 , point * p2){
    point_t * ; - malloc(sizeof(struct line));
    l-> start = p1;
    l-> end =p2;
    return 1;
}